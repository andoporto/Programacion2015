#ifndef FUNCIONES_H_
#define FUNCIONES_H_

#include "pila.h"


/** usted debe hacer sus propias versiones de ... */
int esAnagrama_2(const char *s1, const char *s2);
char *aHexadecimal_2(unsigned n, char *cad);

/** ... reemplazándolas por ... */
int esAnagrama(const char *s1, const char *s2);
char *aHexadecimal(unsigned n, char *cad);
int es_letra(const char );
char convertir_minuscula(char);

#endif
